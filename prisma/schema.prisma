
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}


//SNAKE-GAME
model Scores {
  id  Int @id @default(autoincrement())
  player  String
  score   Int
  createdAt DateTime @default(now())
}